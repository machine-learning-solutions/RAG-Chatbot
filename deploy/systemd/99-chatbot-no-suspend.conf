# Chatbot host mode — laptop stays awake; screen lock/blank still works.
# Managed by: sudo bash deploy/systemd/enable-host-mode.sh

[Login]
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleSuspendKey=ignore
HandleHibernateKey=ignore
IdleAction=ignore
IdleActionSec=0
